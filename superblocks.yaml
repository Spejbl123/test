image: node:10.15.0

stages:
  - build
  - test
  - deploy

before_script:
  - npm install -g truffle
  - npm install -g ganache-cli
  - npm install

build:
  stage: build
  script:
    - truffle compile

test:
  stage: test
  script:
    - truffle test
